{
	"name": "@gajae-code/gjc-mcp",
	"version": "0.9.6",
	"private": true,
	"type": "module",
	"description": "Fixed standalone GJC runtime updater",
	"bin": {
		"gjc-mcp": "./dist/gjc-mcp"
	},
	"scripts": {
		"check": "biome check . && bun run check:types",
		"check:types": "tsgo -p tsconfig.json --noEmit",
		"test": "bun test",
		"build": "bun build ./src/main.ts --compile --outfile ./dist/gjc-mcp"
	},
	"devDependencies": {
		"@types/bun": "catalog:"
	},
	"engines": {
		"bun": ">=1.3.14"
	}
}
